# Production Environment Configuration
ENVIRONMENT=prod

# Domain configuration
DOMAIN=yourdomain.com

# Container naming prefix (empty for production)
CONTAINER_PREFIX=

# Database configuration
DB_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=itx_db

# SSL Certificate configuration
# Email для уведомлений Let's Encrypt (обязательно для получения сертификата)
CERTBOT_EMAIL=admin@yourdomain.com

# Nginx configuration
NGINX_CONFIG_FILE=prod.conf
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# Nginx entrypoint (default for production)
NGINX_ENTRYPOINT=/docker-entrypoint.sh
NGINX_COMMAND=nginx -g 'daemon off;'

